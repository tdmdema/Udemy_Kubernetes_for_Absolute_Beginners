apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: NodePort
  ports:
    - targetPort: 80 # port on the service
      port: 80 # port of the pod - REQUIRED
      nodePort: 30008 # port to expose on the node
  selector:
    app: myapp
    type: front-end